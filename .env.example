# EcoTrace is fully client-side and requires NO environment variables or secrets to run.
#
# This file documents that fact intentionally. If you later add an optional
# integration (e.g. an analytics endpoint or an AI tips service), declare its
# variables here with placeholder values and NEVER commit the real .env file.
#
# Example (currently unused):
# NEXT_PUBLIC_ANALYTICS_ID=
